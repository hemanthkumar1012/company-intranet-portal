'use client';
import {useState} from 'react';import Nav from '../components/Nav';import {api} from '../../lib/api';
export default function Assistant(){const [q,setQ]=useState(''),[answer,setAnswer]=useState(''),[error,setError]=useState('');const ask=async e=>{e.preventDefault();try{const {data}=await api.post('/ai/policy-ask',{question:q});setAnswer(data.answer);setError('')}catch(e){setError(e.response?.data?.message||'Failed')}};return <main className="container"><Nav/><div className="card"><h1>AI Policy Assistant</h1><form onSubmit={ask}><input className="input" placeholder="Ask about attendance, leave, etc." value={q} onChange={e=>setQ(e.target.value)} required/><button className="btn" type="submit">Ask</button></form>{error&&<p className="error">{error}</p>}{answer&&<div className="card"><strong>Answer</strong><p>{answer}</p></div>}</div></main>}
