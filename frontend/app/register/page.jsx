'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import {api,saveSession} from '../../lib/api';
export default function Register(){const r=useRouter();const [f,setF]=useState({name:'',subdomain:'',adminName:'',email:'',password:'',phone:''});const [msg,setMsg]=useState('');const submit=async(e)=>{e.preventDefault();try{const {data}=await api.post('/auth/company-register',f);saveSession(data);r.push('/admin')}catch(err){setMsg(err.response?.data?.message||'Registration failed')}};return <main className="container"><div className="card" style={{maxWidth:600,margin:'30px auto'}}><h1>Register Company</h1><form onSubmit={submit}>{[['name','Company name'],['subdomain','Subdomain'],['adminName','Admin name'],['email','Admin email'],['password','Password'],['phone','Admin phone']].map(([k,p])=><input key={k} className="input" placeholder={p} type={k==='password'?'password':k==='email'?'email':'text'} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} required={k!=='phone'}/>)}<button className="btn" type="submit">Create company</button></form>{msg&&<p className="error">{msg}</p>}</div></main>}
