'use client';
import {useEffect,useState} from 'react';import Nav from '../components/Nav';import {api} from '../../lib/api';
export default function Admin(){const [rows,setRows]=useState([]),[msg,setMsg]=useState(''),[f,setF]=useState({name:'',email:'',password:'',empId:'',phone:'',department:''});const load=async()=>{try{const {data}=await api.get('/attendance/today-all');setRows(data)}catch(e){setMsg(e.response?.data?.message||'Please login as admin')}};useEffect(()=>{load()},[]);const add=async(e)=>{e.preventDefault();try{await api.post('/auth/add-employee',f);setMsg('Employee added');setF({name:'',email:'',password:'',empId:'',phone:'',department:''})}catch(e){setMsg(e.response?.data?.message||'Failed')}};return <main className="container"><Nav/><h1>Admin Dashboard</h1><div className="grid grid2"><div className="card"><h3>Total Employees Present</h3><strong style={{fontSize:32}}>{rows.length}</strong></div><div className="card"><h3>Today</h3><strong>{new Date().toLocaleDateString()}</strong></div></div><div className="card"><h2>Add Employee</h2><form onSubmit={add} className="grid grid2">{[['name','Name'],['email','Email'],['password','Password'],['empId','Employee ID'],['phone','Phone'],['department','Department']].map(([k,p])=><input key={k} className="input" placeholder={p} type={k==='password'?'password':k==='email'?'email':'text'} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} required={['name','email','password','empId'].includes(k)}/>)}<button className="btn" type="submit">Add Employee</button></form>{msg&&<p>{msg}</p>}</div><div className="card"><h2>Today Attendance</h2><table className="table"><thead><tr><th>Employee</th><th>ID</th><th>Check In</th><th>Check Out</th><th>Source</th></tr></thead><tbody>{rows.map(x=><tr key={x._id}><td>{x.userId?.name}</td><td>{x.userId?.empId}</td><td>{x.checkIn?new Date(x.checkIn).toLocaleTimeString():'-'}</td><td>{x.checkOut?new Date(x.checkOut).toLocaleTimeString():'-'}</td><td>{x.source}</td></tr>)}</tbody></table></div></main>}
